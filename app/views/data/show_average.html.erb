<h1>Перевірка гіпотези про збіг середніх значень двох залежних вибірок</h1>
<br>
<%= link_to 'Back', data_path %>
<br>
<div class="container">
  <table class="table table-bordered table-striped table-left">
    <thead>
    <tr class="info">
      <th>x</th>
      <th>y</th>
      <th>z = x - y</th>
      <th>z − Z</th>
      <th>(z − Z)^2</th>
    </tr>
    </thead>

    <tbody>
    <% @x.each_with_index do |x_i, i|  %>
      <tr>
        <td><%= x_i %></td>
        <td><%= @y[i] %></td>
        <td><%= @z[i]  %></td>
        <td><%= @subtracts[i]%></td>
        <td><%= @sqrt_subtracts[i]%></td>
      </tr>
    <% end %>
    </tbody>
  </table>

  <div class="info">
    <p>Z = <%= @avz %> - z среднє</p>
    <p>S(z-Z)^2 = <%= @medium_square %> - середньоквадратичне відхилення</p>
    <p>t = <%= @statistic %> - статистика</p>
    <p>T = <%= @student_quantile %> - квантиль розподілу Стьюдента</p>
    <h3>
      Висновок:
      <%= @conclusion ? 'Cередні двох вибірок збігаються (t <= T)': 'Cередні двох вибірок  не збігаються (t > T)' %>
    </h3>
  </div>
</div>