<h1>Перевірка гіпотези про збіг середніх значень двох залежних вибірок</h1>
<br>
<%= link_to 'Back', data_path %>
<br>
<div class="container">
  <table class="table table-bordered table-striped table-left">
    <thead>
    <tr class="info">
      <th>x</th>
      <th>y</th>
      <th>z = x - y</th>
      <th>α</th>
      <th>|z|</th>
      <th>r – ранги для|z|</th>
      <th>α⋅r</th>
</tr>
    </thead>

<tbody>
<% @x.each_with_index do |x_i, i|  %>
      <tr>
        <td><%= x_i %></td>
        <td><%= @y[i] %></td>
        <td><%= @z[i]  %></td>
        <td><%= @a[i]%></td>
        <td><%= @abs_z[i]%></td>
        <td><%= @rank[i] %></td>
        <td><%= @multiple_a_rank[i] %></td>
      </tr>
<% end %>
</tbody>
  </table>

<div class="info">
  <p>T = <%= @statistic %> - статистика</p>
  <p>E{T} <%= @excess %> - коефіцієнт ексцесу</p>
  <p>D{T} = <%= @dispersion%> - дисперсія</p>
  <p>A = <%= @bunch.to_a %> </p>
  <p>u = <%= @standard_statistic %> - стандартизована статистика</p>
  <h3>
    Висновок: <%= @conclusion %>
  </h3>
  </div>
</div>