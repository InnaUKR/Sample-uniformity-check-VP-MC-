<h1>Критерії перевірки збігу середніх для двох незалежних вибірок</h1>
<br>
<%= link_to 'Back', data_path %>
<br>
<div class="info">
  <p>W = <%= @statistic %> -  статистика критерію - величина, що дорівнює сумі рангів елементів вибірки x</p>
  <p>u = <%= @standardized_statistics %> - стандартизована статистика</p>
  <p>E { W } = <%= @e %> </p>
  <p>D { W }= <%= @d %> </p>
  <h3>
    Висновок:
    <%= @conclusion %>
  </h3>
</div>

<div class="container">
  <table class="table table-bordered table-striped table-left">
    <thead>
    <tr class="info">
      <th>x</th>
      <th>y</th>
    </tr>
    </thead>

    <tbody>
    <% @x.each_with_index do |x_i, i|  %>
      <tr>
        <td><%= x_i %></td>
        <td><%= @y[i] %></td>
      </tr>
    <% end %>
    </tbody>
  </table>

  <table class="table table-bordered table-striped table-left">
    <thead>
    <tr class="info">
      <th>Спільна вибірка</th>
      <th>Порядкові номери</th>
      <th>Ранги</th>
    </tr>
    </thead>

    <tbody>
    <% @sample.each do |element|  %>
      <tr>
        <td><%= element.value %></td>
        <td><%= element.index %></td>
        <td><%= element.rank %></td>
      </tr>
    <% end %>
    </tbody>
  </table>


</div>