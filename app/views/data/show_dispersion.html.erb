<h1>Перевірка гіпотези про збіг дисперсій двох вибірок</h1>
<br>
<%= link_to 'Back', data_path %>
<br>
<div class="info">
  <p>X = <%= @avx %> - x среднє</p>
  <p>Y = <%= @avy %> - y среднє</p>
  <p>S(x-X)^2 = <%= @medium_square_x %> - середньоквадратичне відхилення х</p>
  <p>S(y-Y)^2 = <%= @medium_square_y %> - середньоквадратичне відхилення y</p>
  <p>f = <%= @statistic %> - статистика</p>
  <p>F = <%= @fisher_quantile %></p>
  <p>F - квантиль розподілу Фішера</p>
  <h3>
    Висновок:
    <%= @conclusion ? 'Дисперсії двох вибірок збігаються(f <= F)': 'дисперсії двох вибірок НЕ збігаються(f > F)' %>
  </h3>
</div>

<div class="container">
  <table class="table table-bordered table-striped table-left">
    <thead>
    <tr class="info">
      <th>x</th>
      <th>x- X</th>
      <th>(x − X)^2</th>
    </tr>
    </thead>

    <tbody>
    <% @x.each_with_index do |x_i, i|  %>
      <tr>
        <td><%= x_i %></td>
        <td><%= @subtracts_x[i] %></td>
        <td><%= @sqrt_subtracts_x[i]  %></td>
      </tr>
    <% end %>
    </tbody>
  </table>

  <table class="table table-bordered table-striped table-left">
    <thead>
    <tr class="info">
      <th>y</th>
      <th>y- Y</th>
      <th>(y − Y)^2</th>
    </tr>
    </thead>

    <tbody>
    <% @y.each_with_index do |y, i|  %>
      <tr>
        <td><%= y %></td>
        <td><%= @subtracts_y[i] %></td>
        <td><%= @sqrt_subtracts_y[i]  %></td>
      </tr>
    <% end %>
    </tbody>
  </table>


</div>